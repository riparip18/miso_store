[build]
  command = "npm run build"
  publish = "dist"

# Optional: set Node version if needed
#[[plugins]]
# (Add plugins here if you decide later)

[dev]
  command = "npm run dev"
  port = 5173
  publish = "dist"
